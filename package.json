{
  "name": "nyanclaw",
  "version": "0.1.0",
  "description": "Personal agent for task management, schedule management, and experience accumulation",
  "type": "module",
  "module": "src/index.ts",
  "scripts": {
    "dev": "bun run src/index.ts",
    "start": "bun run src/index.ts",
    "lint:secrets": "secretlint 'src/**/*.ts' '*.json' '*.yaml'",
    "lint": "bun run lint:secrets && bun run tsc --noEmit",
    "test": "bun test src/org src/session src/records src/tools src/skim src/grok"
  },
  "dependencies": {
    "@earendil-works/pi-agent-core": "^0.80.3",
    "@earendil-works/pi-ai": "^0.80.3",
    "@earendil-works/pi-tui": "^0.80.3",
    "@opentui/core": "^0.4.3",
    "@opentui/react": "^0.4.3",
    "org-mode-ast": "^0.13.5",
    "react": "^19.2.7"
  },
  "devDependencies": {
    "@secretlint/secretlint-rule-preset-recommend": "^13.0.2",
    "@types/bun": "latest",
    "@types/react": "^19.2.17",
    "secretlint": "^13.0.2",
    "typescript": "^5"
  }
}
